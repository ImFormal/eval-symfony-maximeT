{% extends 'base.html.twig' %}

{% block title %}Ajouter une Task{% endblock %}

{% block body %}
<turbo-frame id="id-task">
    <div>
        {{ form_start(form) }}
        {{ form_rest(form)}}
        {{ form_end(form) }}
    </div>
    {% include "_toast.html.twig" %}
</turbo-frame>
{% endblock %}